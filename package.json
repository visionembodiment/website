{
  "name": "vision-embodiment-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "rm -rf .next && next dev --turbopack",
    "dev:mobile": "rm -rf .next && IP=$(ifconfig | grep 'inet ' | grep -v 127.0.0.1 | awk '{print $2}' | head -1) && echo '\n📱 Mobile URL: http://'$IP':3000\n' && next dev --turbopack --hostname 0.0.0.0",
    "build": "next build --turbopack",
    "start": "next start",
    "start:mobile": "IP=$(ifconfig | grep 'inet ' | grep -v 127.0.0.1 | awk '{print $2}' | head -1) && echo '\n📱 Production URL: http://'$IP':3000\n' && next start --hostname 0.0.0.0",
    "lint": "eslint",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "prepare": "husky"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^24.7.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^5.0.4",
    "eslint": "^9",
    "eslint-config-next": "^15.5.4",
    "husky": "^9.1.7",
    "jsdom": "^27.0.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
